const toastClassName = 'avenir center pa3 lh-copy';

const toastOptions = {
	success: {
		className: `${toastClassName} bg-blue`,
	},
	error: {
		className: `${toastClassName} bg-red`,
	},
};

export default toastOptions;
