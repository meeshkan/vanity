const WEEKLY_METRICS = require('./weekly-metrics');
const SAMPLE_METRICS = require('./sample-metrics');
const USER = require('./user');

module.exports = {
	WEEKLY_METRICS,
	SAMPLE_METRICS,
	USER,
};
